import React from 'react'

export interface LoginUserData {
    email: string;
    password: string;
  }

const Login = ()   => {
    return (
        <div>
        <h1>Login</h1>
        </div>
    )
}

export default Login